# GitHub Profile Setup

Put these files in the root of your special profile repository:

```text
Varun-Mangwani/
├── README.md
└── assets/
    ├── profile.png
    ├── cpp-memory-mgm.svg
    ├── python-libraries.svg
    ├── ai-skills.svg
    └── dsa-main-cpp.svg
```

The README uses:
- your uploaded profile artwork as a local asset
- animated typing text
- animated header/footer SVGs
- live GitHub stats
- four local project posters
- links to your actual repositories

Create the profile repository with the exact name `Varun-Mangwani` if it does not already exist, then push these files.
